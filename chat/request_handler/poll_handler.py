connections_map = {}


# with this request we must
# connections_map[message.sender] = conn
# then we use connections_map to update other users data
def handle_poll_request(conn, message):
    # todo: update connections_map
    pass
